# Pointer

Canonical context lives in the `adamastorx` repo:
`.claude/PROJECT.md` and `.claude/WORKFLOW.md`.

This repo (`platform`) = infrastructure concern. Primary agent:
`platform-engineer` (see `adamastorx/.claude/agents/platform-engineer.md`).
